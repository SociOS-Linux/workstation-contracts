[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "workstation-contracts-ipc-reference"
version = "0.1.0"
description = "IPC v0 reference conformance harness for workstation contracts"
requires-python = ">=3.10"
